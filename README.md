# SRIF-CT-SNN
This repository provides core code references for the SRIF-CT-SNN architecture, aiming to facilitate reproducible research and future extensibility. Different arguments need to be set in the main program (mainEEG.py) to use different types of models and training methods.
